import { describe,expect,it } from 'vitest'; import Decimal from 'decimal.js'; import { recordPaymentSchema,reverseChargeSchema } from '../unified-billing-final.validation.js';
describe('unified billing final workflows',()=>{it('rejects allocations above payment amount at service invariant level',()=>{const total=new Decimal('100.00'),allocated=new Decimal('100.01');expect(allocated.gt(total)).toBe(true);});it('accepts exact PKR payment contracts',()=>{expect(recordPaymentSchema.parse({accountId:'507f1f77bcf86cd799439011',amount:'1250.50',currency:'PKR',method:'CASH',allocations:[],externalReference:null,cashierId:null,shiftId:null,counterId:null,receivedAt:'2026-07-21T10:00:00+05:00',idempotencyKey:'payment-unique-key-0001'}).amount).toBe('1250.50');});it('requires either reversal quantity or amount',()=>{expect(reverseChargeSchema.safeParse({quantity:null,amount:null,reasonCode:'ERROR',reason:'Incorrect charge',approvalId:null,expectedVersion:0,idempotencyKey:'reversal-unique-key-1'}).success).toBe(false);});it('reconciles debit and credit amounts exactly',()=>{expect(new Decimal('750.25').minus('750.25').isZero()).toBe(true);});});